# Copy this file and replace the password with your password. Rename it ".env".
# The real .env should never be committed via git.
PORT=5000
MONGO_URI=mongodb://admin:YOUR_PASSWORD@127.0.0.1:27017/workoutplanner?authSource=admin
